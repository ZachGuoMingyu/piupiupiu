// Array.from(‘hello’)
let str = 'hello'
console.log(Array.from(str));

// Array.of() 将一组数值 转为数组
// 只有一个参数
// let result = Array.of(20)  //[20]
// let result = new Array(20) //length=20

// 有多个参数 弥补数组构造函数Array()的不足。因为参数个数的不同，会导致Array()的行为有差异
// let result = Array.of(20, 30, 40)  //[20, 30, 40]
// let result = new Array(20, 30, 40) //[20, 30, 40]
console.log(result);
